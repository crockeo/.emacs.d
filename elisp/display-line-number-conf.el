;; Forcing line numbers to be a consistent width.
(defvar display-line-numbers-width-start)
(setq display-line-numbers-width-start 1)

;; Adding enabling line number by default
(global-display-line-numbers-mode)
