;;; work.el -*- lexical-binding: t; -*-

(ch/pkg work
  ;; This is a placeholder package
  ;; which is populated with work-specific functionality
  ;; that I don't want to leak to GitHub
  ;; and do not want to move between computers.
  )
